# 本地开发密钥文件(wrangler pages dev 会自动读取)
# 把这个文件复制为 .dev.vars 后填入真实值(.dev.vars 已被 .gitignore 忽略,不会提交)

# 后台登录密码,自己改成强密码
ADMIN_PASSWORD=diary2024

# 用于签发登录 token 的随机字符串,改成任意长字符串
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars-long
